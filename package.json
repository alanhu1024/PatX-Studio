{
  "name": "patx",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "codegen": "bash contracts/codegen/gen_ts.sh && bash contracts/codegen/gen_py.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "geist": "^1.4.2"
  }
}